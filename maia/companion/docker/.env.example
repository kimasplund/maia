# Home Assistant connection
HA_URL=http://your-ha-instance:8123
SUPERVISOR_TOKEN=your-supervisor-token

# Valkey configuration
VALKEY_PRIMARY_URL=http://your-primary-valkey:8080
VALKEY_TOKEN=your-valkey-token
VALKEY_URL=http://valkey-replica:8080

# PostGIS configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your-secure-password
POSTGRES_DB=maia
PRIMARY_POSTGIS_HOST=your-primary-postgis-host
PRIMARY_POSTGIS_PORT=5432

# GPU settings
NVIDIA_VISIBLE_DEVICES=all
PYTORCH_CUDA_ALLOC_CONF=max_split_size_mb:512 